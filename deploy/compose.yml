version: '3'

services:
  otel-test-fe:
    container_name: otel-test-fe
    image: otel-test-fe
    networks:
      - test-net
    ports:
      - "800:80"
    depends_on:
      - otel-test-be

  otel-test-be:
    container_name: otel-test-be
    image: otel-test-be
    networks:
      - test-net
    ports:
      - "8080:8080"

networks:
  test-net:
    external: true
